// Página de inicio del repositorio.
// Este es un punto de arranque vacío: cada landing vive en
// app/(landings)/<slug>/. Cuando haya al menos una landing,
// redirigí o convertí esta página en un índice de las mismas.
export default function Home() {
  return <main className="flex-1" />
}
